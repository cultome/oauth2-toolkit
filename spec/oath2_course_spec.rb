# frozen_string_literal: true

RSpec.describe Oath2Toolkit
  it "has a version number" do
    expect(Oath2Toolkit::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
